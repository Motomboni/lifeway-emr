# Local pytest — SQLite (matches CI). Env vars here override backend/.env when running tests.
DEBUG=True
SECRET_KEY=local-test-secret-key-not-for-production
ENFORCE_PLAN_LIMITS=false
REQUIRE_ORGANIZATION_CONTEXT=false
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=local_pytest.sqlite3
